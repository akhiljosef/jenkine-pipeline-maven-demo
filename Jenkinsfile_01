#!groovy

pipeline {
    agent any
    stages {
        stage("Build") {
            steps {
                cmd "mvn -version"
                cmd "mvn clean install"
            }
        }
    }

    post {
        always {
            cleanWs()
        }
    }
}