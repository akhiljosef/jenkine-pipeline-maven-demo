#!groovy

pipeline {
    agent any
    
    stages {
        stage("Build") {
            steps {
                bat "cmd /c mvn -version"
                bat "cmd /c mvn clean install"
            }
        }
    }

    post {
        always {
            cleanWs()
        }
    }
}