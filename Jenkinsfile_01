#!groovy

pipeline {
    agent any
    stages {
        stage("Build") {
            steps {
                "mvn -version"
                "mvn clean install"
            }
        }
    }

    post {
        always {
            cleanWs()
        }
    }
}